__pycache__/
*.pyc
.DS_Store

# Scratch renders from tools/preview.py. The committed build artefacts are the
# two .tosc layouts and their .xml, which are the point of the repo; ad-hoc
# previews are not.
build/*.svg
